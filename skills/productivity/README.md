# Productivity

General workflow tools, not code-specific.

## User-invoked

Reachable only when you type them (Claude Code: `disable-model-invocation: true`; Codex: `policy.allow_implicit_invocation: false` in `agents/openai.yaml`).

- **[second-opinion](./second-opinion/SKILL.md)**: Brief a fresh conversation for a second opinion on the topic this one has narrowed, carrying the case and withholding the first opinion.

## Model-invoked

(none yet)
